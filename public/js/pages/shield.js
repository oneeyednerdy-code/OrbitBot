import { $,api,escapeHtml,state } from '../core.js';import { renderError } from './common.js';
export async function renderShield(){ $('#content').innerHTML='<div class="eyebrow">MODERATION</div><h1 class="page-title">Shield Mode</h1><p class="page-intro">Prepare an anti-raid profile and let Orbit activate it when join spikes or coordinated spam are detected.</p><div id="shieldBody" class="empty">Loading…</div>';try{const d=await api(`/api/guilds/${state.guildId}/shield`),c=d.config||{};let selected=[];try{selected=JSON.parse(c.channel_ids_json||'[]')}catch{};$('#shieldBody').outerHTML=`<div class="grid"><section class="card span-4"><h2>Status</h2><div class="metric">${c.active?'ACTIVE':'READY'}</div><span class="status ${c.active?'roadmap':'active'}">${c.active?'! Shield active':'✓ Normal'}</span></section><section class="card span-8"><h2>Detection</h2><div class="form-grid"><label>Join threshold<input id="shJoin" type="number" value="${c.join_threshold||15}"></label><label>Window (seconds)<input id="shWindow" type="number" value="${c.join_window_seconds||30}"></label><label>Duplicate spam threshold<input id="shDup" type="number" value="${c.duplicate_threshold||6}"></label><label>Mention threshold<input id="shMention" type="number" value="${c.mention_threshold||8}"></label><label>Slowmode seconds<input id="shSlow" type="number" value="${c.slowmode_seconds||30}"></label></div></section><section class="card span-12"><h2>Protected channels</h2><div class="grid">${state.bundle.channels.map(ch=>`<label class="check span-6"><input class="shChannel" type="checkbox" value="${ch.id}" ${selected.includes(ch.id)?'checked':''}>#${escapeHtml(ch.name)}</label>`).join('')}</div><div class="form-grid"><label>Alert channel<select id="shAlert"><option value="">None</option>${state.bundle.channels.map(ch=>`<option value="${ch.id}" ${c.alert_channel_id===ch.id?'selected':''}>#${escapeHtml(ch.name)}</option>`).join('')}</select></label><label>Ping role<select id="shRole"><option value="">None</option>${state.bundle.roles.filter(r=>r.name!=='@everyone').map(r=>`<option value="${r.id}" ${c.alert_role_id===r.id?'selected':''}>${escapeHtml(r.name)}</option>`).join('')}</select></label></div><label class="check"><input id="shEnabled" type="checkbox" ${c.enabled?'checked':''}>Enable Shield Mode</label><label class="check"><input id="shAuto" type="checkbox" ${c.auto_activate!==0?'checked':''}>Allow automatic activation</label><div class="button-row"><button id="saveShield" class="btn secondary">Save Shield Profile</button>${c.active?'<button id="restoreShield" class="btn">Restore Normal</button>':'<button id="activateShield" class="btn">Activate Shield</button>'}</div></section></div>`;$('#saveShield').onclick=async()=>{await api(`/api/guilds/${state.guildId}/shield`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'save',enabled:$('#shEnabled').checked,auto_activate:$('#shAuto').checked,join_threshold:+$('#shJoin').value,join_window_seconds:+$('#shWindow').value,duplicate_threshold:+$('#shDup').value,mention_threshold:+$('#shMention').value,slowmode_seconds:+$('#shSlow').value,channel_ids:[...document.querySelectorAll('.shChannel:checked')].map(x=>x.value),alert_channel_id:$('#shAlert').value,alert_role_id:$('#shRole').value})});renderShield()};$('#activateShield')?.addEventListener('click',async()=>{await api(`/api/guilds/${state.guildId}/shield`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'activate'})});renderShield()});$('#restoreShield')?.addEventListener('click',async()=>{await api(`/api/guilds/${state.guildId}/shield`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'restore'})});renderShield()});}catch(e){renderError(`Shield Mode failed (${e.message}).`)}}
