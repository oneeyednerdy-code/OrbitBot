import { $, api, escapeHtml, state, usableRoles } from '../core.js';
import { renderError } from './common.js';

export async function renderTickets(message=''){
  const guildId=state.guildId;
  $('#content').innerHTML='<div class="eyebrow">SUPPORT</div><h1 class="page-title">Tickets</h1><p class="page-intro">Post a direct ticket button or category dropdown that opens a private support channel.</p><div id="ticketBody" class="empty">Loading…</div>';
  try{
    const data=await api(`/api/guilds/${guildId}/tickets`);
    if(state.guildId!==guildId||state.page!=='tickets'||!$('#ticketBody'))return;
    const channels=state.bundle.channels||[],roles=usableRoles(),enabledCategories=data.categories.filter(category=>Number(category.enabled)===1);
    const categoryOptions=enabledCategories.map(category=>`<option value="${category.id}">${escapeHtml(category.name)}</option>`).join('');
    const categoryCards=data.categories.map(category=>{const questions=parseArray(category.form_json);return `<div class="notice application-form-row"><div><strong>${escapeHtml(category.name)}</strong><br><span class="small">${escapeHtml(category.description||'No description')} · ${questions.length} question${questions.length===1?'':'s'} · ${Number(category.enabled)===1?'Enabled':'Disabled'}</span></div><button class="btn ghost tcEdit" type="button" data-id="${category.id}">Edit</button></div>`}).join('');
    const ticketCards=data.tickets.slice(0,20).map(ticket=>{const reason=ticket.deleted_reason||ticket.closed_reason;return `<div class="notice"><strong>#${ticket.id}</strong> · ${escapeHtml(ticket.status)} · <span class="small">${escapeHtml(ticket.opener_user_id)}</span>${reason?`<br><span class="small">Reason: ${escapeHtml(reason)}</span>`:''}</div>`}).join('');
    $('#ticketBody').outerHTML=`${message?`<div class="notice success">${escapeHtml(message)}</div>`:''}<div class="grid">
      <section class="card span-7"><div class="section-heading"><div><h2 id="categoryEditorTitle">Add category</h2><div class="small">Choose its staff access and up to five Discord modal questions.</div></div><button id="cancelTc" class="btn ghost hidden" type="button">Cancel Edit</button></div><div class="field"><label for="tcName">Name</label><input id="tcName" maxlength="100" placeholder="General"></div><div class="field"><label for="tcDesc">Description</label><input id="tcDesc" placeholder="General support questions"></div><div class="form-grid compact"><label>Emoji (optional)<input id="tcEmoji" maxlength="32" placeholder="🎫"></label><label>Sort order<input id="tcSort" type="number" value="0"></label></div><div class="field"><label for="tcParent">Discord parent category ID (optional)</label><input id="tcParent" placeholder="123456..."></div><div class="field"><label>Staff roles</label><div class="grid">${roles.map(role=>`<label class="check span-6"><input class="tcRole" type="checkbox" value="${role.id}">${escapeHtml(role.name)}</label>`).join('')}</div></div><div class="field"><label for="tcForm">Form questions, one per line (optional)</label><textarea id="tcForm" rows="5" placeholder="What do you need help with?\nWhat have you already tried?"></textarea><div class="small">Discord ticket modals support up to five questions.</div></div><label class="check"><input id="tcEnabled" type="checkbox" checked>Category enabled</label><button id="addTc" class="btn">Add Category</button><div id="categoryStatus" class="notice hidden" aria-live="polite"></div>
      <hr><h2>Post ticket panel</h2><div class="field"><label for="ticketPanelChannel">Discord channel</label><select id="ticketPanelChannel"><option value="">Select…</option>${channels.map(channel=>`<option value="${channel.id}">#${escapeHtml(channel.name)}</option>`).join('')}</select></div><div class="field"><label for="ticketPanelType">Panel type</label><select id="ticketPanelType"><option value="direct">Direct ticket button</option><option value="dropdown">Category dropdown</option></select></div><div id="directCategoryField" class="field"><label for="ticketPanelCategory">Direct ticket category</label><select id="ticketPanelCategory"><option value="">Select…</option>${categoryOptions}</select><div class="small">Clicking the panel button opens this category directly. If it has questions, Discord shows the form first.</div></div><div id="ticketButtonLabelField" class="field"><label for="ticketButtonLabel">Button label</label><input id="ticketButtonLabel" maxlength="80" value="Open Ticket"></div><div class="field"><label for="ticketPanelMessage">Panel message</label><textarea id="ticketPanelMessage" rows="4" maxlength="2000">**Support Tickets**\nNeed help? Open a private ticket below.</textarea></div><button id="postTicketPanel" class="btn secondary">Post Ticket Panel</button><div id="ticketPanelStatus" class="notice hidden" aria-live="polite"></div></section>
      <section class="card span-5"><h2>Categories</h2>${categoryCards||'<div class="empty">No categories yet.</div>'}<div class="callout">After editing a category, repost a category-dropdown panel so its visible labels in Discord match Orbit. Updated questions take effect automatically because the category ID stays the same.</div><h2>Recent tickets</h2>${ticketCards||'<div class="empty">No tickets yet.</div>'}<div class="callout">Close locks the opener from replying and keeps the channel. Delete is staff-only and removes the Discord channel. Both require a reason.</div></section>
    </div>`;
    let editingCategoryId=null;
    $('#ticketPanelType').onchange=updatePanelMode;updatePanelMode();
    document.querySelectorAll('.tcEdit').forEach(button=>button.addEventListener('click',()=>{
      const category=data.categories.find(item=>Number(item.id)===Number(button.dataset.id));if(!category)return;
      editingCategoryId=Number(category.id);$('#categoryEditorTitle').textContent='Edit category';$('#addTc').textContent='Save Changes';$('#cancelTc').classList.remove('hidden');
      $('#tcName').value=category.name||'';$('#tcDesc').value=category.description||'';$('#tcEmoji').value=category.emoji||'';$('#tcSort').value=Number(category.sort_order||0);$('#tcParent').value=category.discord_category_id||'';$('#tcForm').value=parseArray(category.form_json).map(question=>question.label||'').filter(Boolean).join('\n');$('#tcEnabled').checked=Number(category.enabled)===1;
      const selected=new Set(parseArray(category.staff_role_ids_json).map(String));document.querySelectorAll('.tcRole').forEach(input=>{input.checked=selected.has(input.value)});$('#categoryStatus').className='notice hidden';$('#categoryEditorTitle').scrollIntoView({behavior:'smooth',block:'start'});
    }));
    $('#cancelTc').onclick=()=>renderTickets();
    $('#addTc').onclick=async()=>{
      const status=$('#categoryStatus'),wasEditing=Boolean(editingCategoryId);status.className='notice';status.textContent=wasEditing?'Saving category…':'Creating category…';
      const staff_role_ids=[...document.querySelectorAll('.tcRole:checked')].map(input=>input.value),form=$('#tcForm').value.split('\n').map(value=>value.trim()).filter(Boolean).slice(0,5).map(label=>({label,long:true,required:true}));
      try{await api(`/api/guilds/${guildId}/tickets`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'category',id:editingCategoryId,name:$('#tcName').value,description:$('#tcDesc').value,emoji:$('#tcEmoji').value,sort_order:Number($('#tcSort').value||0),discord_category_id:$('#tcParent').value,staff_role_ids,form,enabled:$('#tcEnabled').checked})});if(state.guildId===guildId&&state.page==='tickets')renderTickets(wasEditing?'Category updated. Repost any category-dropdown panel so its visible label matches Orbit.':'Ticket category created.');}
      catch(error){status.className='notice error';status.textContent=error.payload?.detail||`Could not ${wasEditing?'update':'create'} category (${error.message}).`;}
    };
    $('#postTicketPanel').onclick=async()=>{
      const status=$('#ticketPanelStatus');status.className='notice';status.textContent='Posting ticket panel…';
      try{
        const result=await api(`/api/guilds/${guildId}/tickets`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'panel',channel_id:$('#ticketPanelChannel').value,panel_type:$('#ticketPanelType').value,category_id:$('#ticketPanelCategory').value,button_label:$('#ticketButtonLabel').value,message:$('#ticketPanelMessage').value})});
        if(state.guildId!==guildId||state.page!=='tickets')return;
        status.className='notice success';status.innerHTML=`Ticket panel posted successfully. <a href="https://discord.com/channels/${guildId}/${result.channel_id}/${result.message_id}" target="_blank" rel="noopener">View in Discord</a>`;
      }catch(error){status.className='notice error';status.textContent=`${error.payload?.detail||`Could not post the ticket panel (${error.message}).`}${error.payload?.discord_code?` Discord code ${error.payload.discord_code}.`:''}${error.payload?.request_id?` Reference ${error.payload.request_id}.`:''}`;}
    };
  }catch(error){if(state.guildId===guildId&&state.page==='tickets')renderError(`Tickets failed (${error.message}).`)}
}

function updatePanelMode(){const direct=$('#ticketPanelType')?.value==='direct';$('#directCategoryField')?.classList.toggle('hidden',!direct);$('#ticketButtonLabelField')?.classList.toggle('hidden',!direct)}
function parseArray(raw){try{const value=Array.isArray(raw)?raw:JSON.parse(raw||'[]');return Array.isArray(value)?value:[]}catch{return []}}
