# Orbit 0.1.0-alpha.15 — Automation Engine

1. Back up D1.
2. `npm install && npm run typecheck`
3. `npm run db:remote && npm run deploy`
4. Ensure the Discord Gateway is running.
5. Create a low-impact test automation in a private test channel.

The engine revalidates stored actions server-side. Keep automated bans disabled unless you intentionally create them and have tested protected-role behavior.
