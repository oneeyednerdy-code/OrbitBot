import type { Env } from '../../types';import { json } from '../../http/responses';
export async function creatorDirectoryApi(request:Request,env:Env,guildId:string):Promise<Response>{if(request.method==='GET'){const r=await env.DB.prepare('SELECT * FROM creator_directory WHERE guild_id=? ORDER BY display_name').bind(guildId).all();return json({creators:r.results});}if(request.method==='POST'){const b=await request.json<any>();if(!b.display_name)return json({error:'display_name_required'},400);const now=Date.now();if(b.id){await env.DB.prepare('UPDATE creator_directory SET discord_user_id=?,display_name=?,twitch_name=?,youtube_channel_id=?,bio=?,live_role_id=?,approved=?,enabled=?,updated_at=? WHERE id=? AND guild_id=?').bind(b.discord_user_id||null,b.display_name,b.twitch_name||null,b.youtube_channel_id||null,b.bio||null,b.live_role_id||null,b.approved===false?0:1,b.enabled===false?0:1,now,Number(b.id),guildId).run();return json({ok:true});}const r=await env.DB.prepare('INSERT INTO creator_directory(guild_id,discord_user_id,display_name,twitch_name,youtube_channel_id,bio,live_role_id,approved,enabled,created_at,updated_at) VALUES(?,?,?,?,?,?,?,1,1,?,?)').bind(guildId,b.discord_user_id||null,b.display_name,b.twitch_name||null,b.youtube_channel_id||null,b.bio||null,b.live_role_id||null,now,now).run();return json({ok:true,id:Number(r.meta.last_row_id)});}if(request.method==='DELETE'){const id=Number(new URL(request.url).searchParams.get('id'));await env.DB.prepare('DELETE FROM creator_directory WHERE id=? AND guild_id=?').bind(id,guildId).run();return json({ok:true});}return json({error:'method_not_allowed'},405);}
