import { $, api, escapeHtml, state, title, usableRoles } from '../core.js';
import { renderError } from './common.js';

export async function renderLeveling(){
 $('#content').innerHTML='<div class="eyebrow">ENGAGEMENT</div><h1 class="page-title">Leveling</h1><p class="page-intro">Message XP with cooldowns, rewards and a server leaderboard.</p><div id="levelBody" class="empty">Loading…</div>';try{const data=await api(`/api/guilds/${state.guildId}/leveling`);const c=data.config||{};const roles=usableRoles();const channels=state.bundle.channels||[];$('#levelBody').outerHTML=`<div class="grid"><section class="card span-7"><h2>XP settings</h2><label class="check"><input id="lvEnabled" type="checkbox" ${c.enabled?'checked':''}>Enable leveling</label><div class="grid"><div class="field span-4"><label>Min XP</label><input id="lvMin" type="number" value="${c.xp_min||15}"></div><div class="field span-4"><label>Max XP</label><input id="lvMax" type="number" value="${c.xp_max||25}"></div><div class="field span-4"><label>Cooldown sec</label><input id="lvCooldown" type="number" value="${c.cooldown_seconds||60}"></div></div><div class="field"><label>Level-up channel</label><select id="lvChannel"><option value="">Disabled</option>${channels.map(x=>`<option value="${x.id}" ${x.id===c.announce_channel_id?'selected':''}>#${escapeHtml(x.name)}</option>`).join('')}</select></div><h3>Add role reward</h3><div class="grid"><div class="field span-4"><input id="lvRewardLevel" type="number" placeholder="Level"></div><div class="field span-8"><select id="lvRewardRole"><option value="">Role…</option>${roles.map(r=>`<option value="${r.id}">${escapeHtml(r.name)}</option>`).join('')}</select></div></div><button id="saveLevel" class="btn">Save</button></section><section class="card span-5"><h2>Leaderboard</h2>${data.leaders.slice(0,25).map((x,i)=>`<div class="notice"><strong>#${i+1} · ${escapeHtml(x.user_id)}</strong><br><span class="small">Level ${x.level} · ${x.xp} XP</span></div>`).join('')||'<div class="empty">No XP yet.</div>'}</section></div>`;$('#saveLevel').onclick=async()=>{await api(`/api/guilds/${state.guildId}/leveling`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({enabled:$('#lvEnabled').checked,xp_min:Number($('#lvMin').value),xp_max:Number($('#lvMax').value),cooldown_seconds:Number($('#lvCooldown').value),announce_channel_id:$('#lvChannel').value,reward_level:Number($('#lvRewardLevel').value||0),reward_role_id:$('#lvRewardRole').value})});renderLeveling();};}catch(error){renderError(`Leveling failed (${error.message}).`)}
}
