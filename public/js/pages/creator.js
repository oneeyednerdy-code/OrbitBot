import { $,api,escapeHtml,state } from '../core.js';import { renderError } from './common.js';
export async function renderCreator(){ $('#content').innerHTML='<div class="eyebrow">CREATOR</div><h1 class="page-title">Community Alerts</h1><p class="page-intro">Post Twitch, YouTube or RSS activity into a chosen Discord channel with custom messages and optional role pings.</p><div id="creatorBody" class="empty">Loading…</div>';try{const d=await api(`/api/guilds/${state.guildId}/creator`);const channels=state.bundle.channels||[],roles=state.bundle.roles.filter(r=>r.name!=='@everyone');$('#creatorBody').outerHTML=`<div class="grid"><section class="card span-5"><h2>Add alert</h2><label>Platform<select id="caType"><option value="twitch">Twitch Live</option><option value="youtube">YouTube Live</option><option value="rss">RSS Feed</option></select></label><label>Creator / label<input id="caLabel"></label><label>Twitch name, YouTube channel ID, or RSS URL<input id="caValue"></label><label>Discord channel<select id="caChannel">${channels.map(c=>`<option value="${c.id}">#${escapeHtml(c.name)}</option>`).join('')}</select></label><label>Ping role<select id="caRole"><option value="">None</option>${roles.map(r=>`<option value="${r.id}">${escapeHtml(r.name)}</option>`).join('')}</select></label><label>Going-live message<textarea id="caLive">🔴 **{creator} is LIVE!**\n{title}\n{url}</textarea></label><label>Offline message<textarea id="caOffline">💜 **{creator} has finished streaming.**\nCatch up here: {vod_url}</textarea></label><label class="check"><input id="caOff" type="checkbox">Post when stream ends</label><button id="caAdd" class="btn">Add Community Alert</button><div class="small">Variables: {creator}, {title}, {url}, {vod_url}</div></section><section class="card span-7"><h2>Community Alerts</h2>${d.sources.length?d.sources.map(x=>`<div class="notice"><strong>${escapeHtml(x.label)}</strong> · ${escapeHtml(x.source_type)}<br><span class="small">${x.last_live_state?'LIVE · ':''}${x.last_error?`Error: ${escapeHtml(x.last_error)}`:`Channel ${escapeHtml(x.discord_channel_id)}`}</span><button class="btn ghost caDel" data-id="${x.id}">Remove</button></div>`).join(''):'<div class="empty">No alerts configured.</div>'}</section></div>`;$('#caAdd').onclick=async()=>{await api(`/api/guilds/${state.guildId}/creator`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({source_type:$('#caType').value,label:$('#caLabel').value,source_value:$('#caValue').value,discord_channel_id:$('#caChannel').value,mention_role_id:$('#caRole').value,live_message:$('#caLive').value,offline_message:$('#caOffline').value,notify_offline:$('#caOff').checked})});renderCreator()};document.querySelectorAll('.caDel').forEach(b=>b.onclick=async()=>{await api(`/api/guilds/${state.guildId}/creator?id=${b.dataset.id}`,{method:'DELETE'});renderCreator()});}catch(e){renderError(`Community Alerts failed (${e.message}).`)}}
