import { $, api, escapeHtml, state, title, usableRoles } from '../core.js';
import { renderError } from './common.js';

export async function renderCommunity(){
 $('#content').innerHTML='<div class="eyebrow">COMMUNITY</div><h1 class="page-title">Community</h1><p class="page-intro">Welcome/goodbye, auto-role, custom commands, polls, reminders and sticky messages.</p><div id="communityBody" class="empty">Loading…</div>';try{const data=await api(`/api/guilds/${state.guildId}/community`);const c=data.config||{};const channels=state.bundle.channels||[];const roles=usableRoles();const channelOpts=(selected='')=>'<option value="">Disabled</option>'+channels.map(x=>`<option value="${x.id}" ${x.id===selected?'selected':''}>#${escapeHtml(x.name)}</option>`).join('');$('#communityBody').outerHTML=`<div class="grid"><section class="card span-7"><h2>Welcome + Goodbye</h2><div class="field"><label>Welcome channel</label><select id="cmWelcomeChannel">${channelOpts(c.welcome_channel_id)}</select></div><div class="field"><label>Welcome message</label><textarea id="cmWelcome" rows="3">${escapeHtml(c.welcome_message||'Welcome {user}!')}</textarea></div><div class="field"><label>Goodbye channel</label><select id="cmGoodbyeChannel">${channelOpts(c.goodbye_channel_id)}</select></div><div class="field"><label>Goodbye message</label><textarea id="cmGoodbye" rows="3">${escapeHtml(c.goodbye_message||'{username} has left the server.')}</textarea></div><div class="field"><label>Auto-role</label><select id="cmAutorole"><option value="">None</option>${roles.map(r=>`<option value="${r.id}" ${r.id===c.autorole_id?'selected':''}>${escapeHtml(r.name)}</option>`).join('')}</select></div><button id="saveCommunity" class="btn">Save Community Settings</button><hr><h2>Custom command</h2><div class="field"><input id="ccName" placeholder="rules"></div><div class="field"><textarea id="ccResponse" rows="3" placeholder="Command response"></textarea></div><button id="addCommand" class="btn secondary">Save !command</button><hr><h2>Sticky</h2><div class="field"><select id="stickyChannel">${channelOpts('')}</select></div><div class="field"><textarea id="stickyContent" rows="3" placeholder="Remember to check the rules"></textarea></div><button id="saveSticky" class="btn secondary">Save Sticky</button></section><section class="card span-5"><h2>Commands</h2>${data.commands.map(x=>`<div class="notice"><strong>!${escapeHtml(x.command)}</strong><br><span class="small">${escapeHtml(x.response)}</span></div>`).join('')||'<div class="empty">No custom commands.</div>'}<h2>Quick poll</h2><div class="field"><select id="pollChannel">${channelOpts('')}</select></div><div class="field"><input id="pollQuestion" placeholder="Question"></div><div class="field"><textarea id="pollOptions" rows="4" placeholder="Option one\nOption two"></textarea></div><button id="postPoll" class="btn secondary">Post Poll</button></section></div>`;$('#saveCommunity').onclick=async()=>{await api(`/api/guilds/${state.guildId}/community`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'config',welcome_channel_id:$('#cmWelcomeChannel').value,welcome_message:$('#cmWelcome').value,goodbye_channel_id:$('#cmGoodbyeChannel').value,goodbye_message:$('#cmGoodbye').value,autorole_id:$('#cmAutorole').value})});renderCommunity();};$('#addCommand').onclick=async()=>{await api(`/api/guilds/${state.guildId}/community`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'command',command:$('#ccName').value,response:$('#ccResponse').value})});renderCommunity();};$('#saveSticky').onclick=async()=>{await api(`/api/guilds/${state.guildId}/community`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'sticky',channel_id:$('#stickyChannel').value,content:$('#stickyContent').value,every_n_messages:10})});renderCommunity();};$('#postPoll').onclick=async()=>{await api(`/api/guilds/${state.guildId}/community`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({op:'poll',channel_id:$('#pollChannel').value,question:$('#pollQuestion').value,options:$('#pollOptions').value.split('\n').map(x=>x.trim()).filter(Boolean),duration_hours:24})});};}catch(error){renderError(`Community failed (${error.message}).`)}
}
